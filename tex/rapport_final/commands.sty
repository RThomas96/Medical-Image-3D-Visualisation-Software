\NeedsTeXFormat{LaTeX2e}
\ProvidesPackage{commands}
\RequirePackage{FiraSans}
\RequirePackage{FiraMono}
\RequirePackage{helvet}
\RequirePackage{color}
\RequirePackage{libertine}

\renewcommand*\familydefault{\sfdefault}

\newif\ifdraft\draftfalse
\DeclareOption{draft}{
	\drafttrue
}

\ProcessOptions\relax

% Some useful commands when writing :
\providecommand\wip[1]{}
\providecommand\todo[1]{}
\providecommand\commentaire[1]{}
% Not defined if the draft opion is not given.

\ifdraft
	\renewcommand\wip[1]{\texttt{{\color{red}\fontfamily{\firamono@family}\selectfont#1}}}
	\renewcommand\todo[1]{\footnote{\color{red}\textit{TODO : #1}}}
	\renewcommand\commentaire[1]{{\color{blue}\fontfamily{\fira@family}\selectfont\textit{Commentaire : #1}}}
\fi

\endinput
